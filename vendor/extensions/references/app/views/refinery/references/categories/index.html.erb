<% content_for :body do %>
  <ul id="categories">
    <% @categories.each do |category| %>
      <li>
        <%= link_to category.title, refinery.references_category_path(category) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
