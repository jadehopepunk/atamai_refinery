%section#records
  = render 'records'
%aside#actions
  = render 'actions'
= render '/refinery/admin/make_sortable', :tree => false if !searching? and ::Refinery::References::Admin::CategoriesController.sortable? and ::Refinery::References::Category.count > 1
